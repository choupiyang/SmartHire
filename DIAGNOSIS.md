# DIAGNOSIS.md - 智能体系统诊断、测试与自愈法典

> **Version:** 3.0.0 (The Chaos & Evolution Edition)
> **Status:** Active / Enforced
> **Role:** The Immune System / 免疫、诊断与自愈系统
> **Ecosystem:** Aligned with `ARCH.md` (Blueprint), `LAW.md` (Rules), `COMPOUND.md` (History), `MAP.md` (Execution)

> **致测试开发者与架构师：**
> 传统的测试关注”输入 A 是否得到 B”；但智能体测试关注的是：”在通往 B 的无数条路径中，系统是否始终守住了 LAW 的红线（核心禁令：`LAW-ENV-001` 禁止容器化、`LAW-ENV-002` 禁止绝对路径、`LAW-TENANT-001` 禁止多租户、`LAW-ENG-001` 职责物理隔离），并展现了 MAP 预设的智力”。你的工作不是单纯地寻找 Bug，而是在衡量一个会进化的生命体的”智力上限”与”生存底线”。

## 0. 核心哲学：理、象、数、变、乱 (The Core Philosophy)

本法典基于系统论与混沌工程推演而成，是所有 Debug、测试与自动化修复动作的最高指导原则：

* **观象定界:** 报错只是“象”，越权才是“理”。严禁跨维度盲目排查、头痛医头。
* **探数复现:** 代码是确定性的，必须先在确定性环境下验证逻辑。
* **察变溯源:** 系统的状态是流动的，今日之错往往源于昨日之变。
* **混沌抗压:** 现实环境（特别是 LLM）是混沌的。确定性测试通过仅代表逻辑无误，混沌测试通过才代表系统存活。
* **分级自愈:** 赋予 AI 修复的权力，但必须将其关在沙盒的牢笼中。

---

## 1. 诊断维度与观象定界 (Phase 1: The 4-Layer Diagnostic Model & Isolation)

> **目标:** 消除日志幻觉，强制架构对齐，精准定损。

当系统表现异常时，测试者/AI必须迅速将问题定位到以下四个维度之一，严禁跨维度盲目排查：

### 1.1 四层定损模型

* **L1 - 物理层缺陷 (Environment/Physical):** * *现象:* 进程异常退出、IO 超时、句柄泄露、文件锁死。
* *诊断:* 脱离 AI 业务逻辑，检查 Windows 宿主机的物理资源约束（如路径深度是否触发 MAX_PATH、端口/文件多进程竞争）。


* **L2 - 协议层缺陷 (Communication/Protocol):** * *现象:* 指令丢失、字段解析失败、状态机倒挂。
* *诊断:* 审计通信总线中的“信封”数据完整性。收发端的 Pydantic 模型是否严格对齐？是否存在并发指令顺序错乱？


* **L3 - 逻辑层缺陷 (Reasoning/Logic):** * *现象:* 幻觉、死循环、SOP 执行偏离、反思（Reflexion）无效。
* *诊断:* 评估决策链的合理性及 SOP 处理极端边界（Edge Cases）的完备性。


* **L4 - 表现层缺陷 (Persona/Presentation):** * *现象:* 语气崩塌、信息过载、格式破损、违背用户偏好。
* *诊断:* 评估最终输出与用户画像（Soul）的契合度及风格一致性。



### 1.2 悬停协议与案发现场快照

* **悬停协议:** 遇到任何 Bug，AI 工具生成修复方案前必须回答：“该报错本质上违反了 `ARCH.md` 哪一层边界？”严禁生成掩盖报错（如强加 `try-except` 吞异常）的代码。
* **快照隔离:** 严禁仅靠 Terminal 滚动日志排查。系统必须将核心状态覆写入 `logs/debug/` 目录的纯净文件（`1_llm_intent.json` / `2_agent_raw_result.json` / `3_final_response.txt`），通过快照瞬间定位故障环节。

---

## 2. 测试策略：探数、抗压与裁判 (Phase 2: Determinism, Chaos & Judge)

> **目标:** 验证非确定性逻辑在确定性环境下的鲁棒性边界。

### 2.1 确定性边界与故障注入 (Deterministic Boundary)

* **基线验证:** 严禁在对话框用自然语言碰运气排查底层 Bug。必须提取崩溃时的 `1_llm_intent.json` 作为确定性载荷，直接注射给底层 Agent，Agent 需 100% 成功消费。
* **物理故障注入:** 强行注入预设的物理报错（如：磁盘满、网络断开），观察 L3 逻辑层是否能按照 MAP 协议触发有效反思并规避，而非机械重试。

### 2.2 混沌注入协议 (Chaos Injection)

确定性测试通过后，必须对 Agent 进行模拟 LLM 不确定性输出的混沌测试：

* **变异规则:**
* *Whitespace Noise:* 在 JSON 中随机插入空格、换行。
* *Key Shuffling:* 打乱 JSON Key 的顺序。
* *Missing Fields:* 随机剔除非必选字段。
* *Type Drift:* 强制类型漂移（如 `1` -> `"1"`）。


* **验收标准:** 对基线 Payload 进行 100 次随机变异，要求系统崩溃率 = 0%，异常捕获率 = 100%。未通过混沌测试的修复，严禁在 `MAP.md` 中标记 Done。

### 2.3 智能阅卷机制 (LLM-as-a-Judge)

* 放弃死板的“字符匹配”测试。引入独立的“裁判模型”，根据预设的 Rubric 对 L3 与 L4 输出进行逻辑审计，输出包含 Confidence (置信度) 与 Reasoning (判定理由) 的结构化报告。

---

## 3. 察变溯源与 Windows 工具箱 (Phase 3: State Evolution & Tooling)

> **目标:** 掌控有状态系统的慢性中毒风险与物理约束。

### 3.1 状态法医协议与熔断机制

* **只读提审:** 当 `SOUL.md` 或记忆被污染时，让 AI 读取 `.versions/` 目录进行 Diff 分析。**严禁 AI 自动执行**抹除记忆或回滚，所有的状态回滚必须由人类确权。
* **拔管原则:** 动态生成的衍生代码须标记 `[Experimental]`。发生资源泄漏或死循环时，第一原则是“拔管”禁用衍生代码。严禁在未隔离衍生代码的情况下，修改核心基座。

### 3.2 Windows 物理机特有诊断工具箱

测试开发者必须熟练使用以下工具监控原生系统：

* **Process Monitor (Sysinternals):** 实时捕获文件系统与注册表异常，定位绝对路径硬编码与 IO 越权。
* **Redis-cli Monitor:** 观测进程间通信频率与载荷大小，定位认知同步延迟与总线风暴。
* **Temporal Tracing:** 利用全局 `trace_id` 将跨进程的碎片化日志（如 SQLite 偏好、LanceDB 技能、DuckDB 流水）聚合成完整的因果链。

---

## 4. 风险定级与自愈 (Phase 4: Tiered Risk & Auto-Healing)

> **目标:** 在保障 LAW 宪法底线的前提下，实现安全可控的自动化修复。

### 4.1 技术债与风险等级评估 (Risk Ranking)

* **P0 (毁灭级):** 违反 LAW 核心禁令：
  - 使用容器化技术（违反 `LAW-ENV-001`）
  - 硬编码绝对路径（违反 `LAW-ENV-002`）
  - 引入多租户/RBAC 管理（违反 `LAW-TENANT-001`）
  - 跨职责物理隔离（违反 `LAW-ENG-001`）
  - 涉及 `ARCH.md` 越权或主进程崩溃。
  **【权限：强制人工熔断，AI 仅可生成提案】**
* **P1 (严重级):** 反思回路失效，系统遇错无法自愈导致逻辑停滞或死循环。**【权限：强制人工介入排查】**
* **P2 (一般级):** 工具解析失败、API 格式变动、`Experimental` 技能报错、L4 表现层风格偏离。**【权限：允许 AI 全自动生成修复，进入沙盒彩排】**

### 4.2 沙盒彩排与回归测试铁律 (Sandbox & Non-Degradation)

任何自动修复或 SOP 升级，必须经历以下强制关卡：

1. **暗影分支:** 在隔离分支中修改代码。
2. **确定性与混沌验证:** 依次通过 [2.1 确定性验证] 与 [2.2 混沌注入验证]。
3. **全量回归 (不退化原则):** 必须跑通 **金牌测试集 (Gold Dataset)**（包含历史所有已解决的极端案例）。如果为修复 Bug A 导致曾经成功的案例 B 失败（逻辑排异），该任务立即驳回。
4. **安全合并:** 沙盒测试全绿且 Diff 未触碰 Core Engine 时，方可自动热重载。